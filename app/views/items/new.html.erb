<%= simple_form_for @item do |f| %>
  <%= f.association :category %>
  <%= f.input :color %>
  <%= f.file_field :image, accept: "image/x-png,image/jpeg", capture: "camera", id: "camera"  %> <br>
  <%= f.button :submit, class: 'btn-primary' %>
<% end %>
