<div class= "container-gray">
  <% if  params[:favorite]%>
    <%= link_to "recent", items_path %>
  <% else %>
    <%= link_to "favorite", items_path(favorite:true) %>
  <% end %>
  <h2 id="mycolors">My colors</h2>
  <div class="items-index">
    <% @items.select(:color).pluck(:color).uniq.each do |color| %>
      <%= link_to items_path(color:color), class: "no-underline" do %>
        <div class="color" style="background-color: <%=color%>">
          <% if Item.where(color:color, favorite:true).exists? %>
            <i class="heart fas fa-heart"></i>
          <% else  %>
            <i class="heart far fa-heart"></i>
          <% end %>
        </div>
      <% end %>
    <% end %>
  </div>
</div>

<!-- We want the most recent colors to be the first to be displayed whether it is the favorite or recent view.
Therefore the first order of business is to make the sorting of the array based on age happen in the controller.
Then add something to only display the favorited items. -->
